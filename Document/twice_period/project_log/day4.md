DAY4：

剩下的要写的：

```
通信协议的重构，解决消息广播和消息私发的问题
客户端的文件传输模块
```



> 闲聊：跨线程的信号和槽的通信方式是否可以用临界区和多线程之间的绑定关系来实现，也就是说用临界区作为线程之间的通信方式，来实现信号和槽？



因为客户端和服务端的用户列表要实时加入和删除，故用QListView

而像聊天信息，日志这些则不需要，用Widget

不如改名为“临时会议室”



现在发现tcp流的读取出了问题，出现很多延迟，需要优化tcp的读取，这可能是这个项目的缺点了，不想改，太繁琐了

> 下次网络流我需要创建多字节缓存读取，然后再读缓存---->多级缓存读取

需要先更改协议类型



现在正在重构了，我打算交给AI去重构，不然实在是太耗时间了，协议由我来设计

AI写的还是有些地方有缺陷，我需要记录缺点位置并修改：

```
server端历史记录发送
server端flag分支没有注释
subType分支下面没有充满注释
tellPoint那个函数我要自己重写，然后相关的通知再改下：
	踢人，addNewSharedFile，addNewPrivateFile
	
client端：
核心(receiveMsg):
	被踢分支注释补充
	CHAT_USER_OFFLINE/CHAT_USER_JOIN 分支注释实时更新
```

评审：

```
私发中的用户名读取可能存在性能问题，我需要约束用户的取名长度，20字以内吧，这应该既保证用户体验也平衡性能，即使有大量的私发应该也没事(假如这里做极致性能，那么我会选择再开一个端口号来异步接收私发的用户名(直接发用户名，发送方自带地址))
致命：文件传输模块中，查询的宏不一致！！！【不过还好这次没有重构文件传输模块】

```





size + INTERUPT + body 应该是最好的设计了

网络开发还是需要学会cv，复制逻辑模块





差不多完成了，总结下剩下的几个点，后续再搞

```
server端历史记录发送
server端flag分支没有注释
subType分支下面没有充满注释
tellPoint那个函数我要自己重写，然后相关的通知再改下：
	踢人，addNewSharedFile，addNewPrivateFile
	
client端：
核心(receiveMsg):
	被踢分支注释补充
	CHAT_USER_OFFLINE/CHAT_USER_JOIN 分支注释实时更新
	
重构FileTransferer,修改服务端接收文件
(修改AI发现的逻辑错误，可能是忘了改的)
```

