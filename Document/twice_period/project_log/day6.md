Day6:

1. 先解决私信的响应问题，添加一条调用即可，响应到聊天栏中，方便调试和用户交互

2. 文件发送问题和文件接收问题：
   问题调试预测：客户端的相关的信号和槽没有响应

   原因：QFileInfo对象没有在 Qt 元对象系统中注册，无法用于跨线程的队列连接

   解决方案

   ```
   改参数为QString来表示地址
   ```

   解决成功，服务器可以接收文件了

3. 文件接收问题

   客户端选定文件后，点击下载后，服务器会崩溃，客户端可以正常交互，但收不到服务器的响应

   原因：可能是死锁，因为服务端在Tdoransfer获取锁这个代码中跳出，没有继续执行

   ```
   同样的，删除多余的锁
   ```

目前模块已全部修复完毕，开始第三轮测试

模块的功能已确认没有问题，项目功能方面暂时完结

细节优化

1. 客户端收取文件时会下载到父文件夹，且会修改文件名

   > 修复完成

2. 服务端下载文件时会文件名命名错误

   > 修复完成

3. 服务器再添加一个设置下载地址的功能

   > 完成

4. 客户端首包单独弄个协议族，防止和心跳包冲突

   > 完成

5. 服务端在存储文件是用[share]和[private]命名区分，防止出现share和private文件重名情况

   > 完成

